export function createVao(gl:WebGL2RenderingContext,buffer:WebGLBuffer,location:number):WebGLVertexArrayObject{const vao=gl.createVertexArray();if(!vao)throw new Error("VAO allocation failed");gl.bindVertexArray(vao);gl.bindBuffer(gl.ARRAY_BUFFER,buffer);gl.enableVertexAttribArray(location);gl.vertexAttribPointer(location,3,gl.FLOAT,false,12,0);gl.bindVertexArray(null);return vao;}// TODO: set actual format, stride, offset, index binding, and divisors.
