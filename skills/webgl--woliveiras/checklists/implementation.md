# Implementation

- [ ] Request WebGL 2; use WebGL 1 only through an explicit maintained path.
- [ ] Check every shader compile and program link with complete logs and cleanup.
- [ ] Establish program, VAO, buffers, framebuffer, textures, viewport, and pass state.
- [ ] Verify texture rules, pixel store, framebuffer completeness, and extension gates.
- [ ] Sync drawing-buffer size, viewport, and all size-dependent attachments.
- [ ] Track deletion and deterministic context-loss restoration.
