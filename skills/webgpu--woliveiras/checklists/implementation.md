# Implementation

- [ ] Handle missing `navigator.gpu`, missing adapter, and rejected device request.
- [ ] Request only used features and necessary limits; attach loss/error handlers.
- [ ] Configure canvas format/alpha and resize all dependent attachments.
- [ ] Verify usage flags, buffer sizes/offsets, CPU/WGSL alignment, and draw bounds.
- [ ] Match shader bindings, layouts, views, samplers, targets, and pipeline state.
- [ ] Reuse resources and define cleanup/fallback behavior.
